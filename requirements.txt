# Pinned runtime deps (mirror of pyproject [project].dependencies).
# For RAG / cloud auth extras install: pip install -e ".[rag,azure,aws]"
mcp>=1.2.0
kubernetes>=29.0.0
httpx>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
pyyaml>=6.0
python-json-logger>=2.0
tenacity>=8.2
